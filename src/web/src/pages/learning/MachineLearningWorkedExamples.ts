export type MLWorkedExample = {
  setup: string;
  substitution: string;
  result: string;
  interpretation: string;
};

const E = (setup: string, substitution: string, result: string, interpretation: string): MLWorkedExample => ({ setup, substitution, result, interpretation });

const EXAMPLES: Record<string, MLWorkedExample> = {
  "经验风险最小化 ERM": E("某分类器在 5 个样本上的成本损失为 0.10、0.30、0.20、0.40、0.20。", "R̂ = (0.10+0.30+0.20+0.40+0.20) / 5", "经验风险 R̂ = 0.24。", "只能说明它在这 5 个训练样本上的平均成本是 0.24；还要与其他模型在时间样本外的风险比较。"),
  "正则化风险最小化": E("模型 A 的训练风险为 0.18，复杂度 Ω=0.40，设 λ=0.10。", "目标值 = 0.18 + 0.10×0.40", "正则化目标 = 0.22。", "若模型 B 的训练风险稍高但正则化目标低于 0.22，应优先比较 B 的样本外稳定性。"),
  "时间序列交叉验证": E("三个先训练后验证时间折的损失分别为 0.18、0.22、0.20。", "CV_time = (0.18+0.22+0.20) / 3", "平均折外损失 = 0.20，最差折 = 0.22。", "报告平均数还不够；最差折暴露了模型在某段制度中的风险。"),
  "学习排序": E("资产 i 的分数 1.2、资产 j 的分数 0.7，真实标签要求 i 排在 j 前，间隔设为 1。", "ℓ = max[0, 1−(1.2−0.7)]", "成对排序损失 = 0.50。", "顺序虽然正确，但分数间隔不足；模型仍会被推动去拉开两者。"),
  "Beta-Binomial": E("先验 Beta(2,2)，随后观察 10 次突破，其中 8 次成功。", "后验 = Beta(2+8, 2+10−8) = Beta(10,4)", "后验均值 = 10/(10+4) ≈ 71.4%。", "样本胜率是 80%，但先验收缩后为 71.4%；小样本不能直接当作精确胜率。"),
  "朴素贝叶斯": E("真实突破先验为 20%；两个证据在真突破下联合近似似然为 0.8×0.6，在失败下为 0.3×0.1。", "真突破权重=0.2×0.8×0.6=0.096；失败权重=0.8×0.3×0.1=0.024", "归一化后 P(真突破|证据)=0.096/(0.096+0.024)=80%。", "80% 依赖条件独立近似；若两个证据高度重复，概率会过度自信。"),
  "Bayes factor": E("数据在候选模型 M1 下的边际似然是 0.12，在基准 M0 下是 0.03。", "BF₁₀ = 0.12 / 0.03", "Bayes factor = 4。", "当前数据在 M1 下出现的可能性是 M0 的 4 倍；这不是收益为正的 4 倍保证。"),
  "Bootstrap": E("对同一因子做 5 次分块重采样，得到 IC：0.04、0.07、0.01、0.05、0.03。", "均值=0.04；SE_boot = sd(0.04,0.07,0.01,0.05,0.03)", "bootstrap 标准误约为 0.022。", "IC 0.04 相对误差不小，应继续报告区间，而不是只展示均值。"),
  "基础 Monte Carlo": E("五条模拟路径的终值收益为 1%、2%、0%、3%、4%。", "E[R] ≈ (1+2+0+3+4)% / 5", "模拟期望收益 = 2%。", "这里只演示样本平均；真实尾部估计通常需要成千上万路径和 Monte Carlo 误差。"),
  "Linear regression": E("模型为 ŷ=0.5%+0.02x₁−0.01x₂，当前 x₁=1、x₂=0.5。", "ŷ = 0.5% + 0.02×1 − 0.01×0.5", "预测值 = 2.0%。", "这是条件均值预测，不是下一期一定获得 2%；还要结合残差分布和成本。"),
  "Ridge regression": E("候选系数 β=(0.6,−0.4)，平方误差 0.10，λ=0.20。", "目标=0.10+0.20×(0.6²+0.4²)", "Ridge 目标 = 0.204。", "大系数会额外付出 0.104 的惩罚，促使相关特征的估计更保守。"),
  "Logistic regression": E("当前线性分数 z=0.8，交易阈值设为 65%。", "P=1/(1+e⁻⁰·⁸)", "上涨概率约 69.0%，高于 65% 阈值。", "模型给出候选行动，但阈值必须由样本外校准、盈亏比和成本共同确定。"),
  "Perceptron": E("初始 w=(0,0)，错分正样本 x=(2,1)，y=+1，学习率 η=0.1。", "w_new = (0,0)+0.1×1×(2,1)", "更新后 w=(0.2,0.1)。", "权重朝正确分类该样本的方向移动；若数据不可线性分割，更新会持续震荡。"),
  "Probit / GLM": E("Poisson GLM 使用 log 连接，当前线性预测子为 0.4。", "log μ=0.4，因此 μ=e⁰·⁴", "条件期望计数 μ≈1.49。", "输出是该条件下的平均事件数，不应解释为一定发生 1.49 次。"),
  "LDA / QDA": E("一维 LDA 中两类均值 μ0=0、μ1=1，共享方差为 1，先验相同，当前 x=0.8。", "δ0=0；δ1=0.8−0.5=0.3", "δ1>δ0，因此判为第 1 类。", "分类来自共享协方差下的相对判别分数；QDA 允许两类方差不同，边界会弯曲。"),
  "Gaussian mixture": E("两个成分权重为 0.7/0.3；某样本在两成分下密度为 0.1/0.4。", "责任度₂=(0.3×0.4)/(0.7×0.1+0.3×0.4)", "样本属于成分 2 的概率约 63.2%。", "应保留 63.2% 的软归属，不能事后把成分 2 武断命名为牛市。"),
  "EM algorithm": E("三个样本为 0、2、10，对高均值成分的责任度为 0.1、0.2、0.9。", "μ_high=(0×0.1+2×0.2+10×0.9)/(0.1+0.2+0.9)", "M 步更新 μ_high≈7.83。", "责任度先来自 E 步，参数再在 M 步更新；不同初值可能收敛到不同局部解。"),
  "Mixture of experts": E("门控权重为 0.7/0.3，两个专家预测收益为 2%/−1%。", "ŷ=0.7×2%+0.3×(−1%)", "组合预测 = 1.1%。", "最终预测同时取决于专家和门控；应分别检查门控是否稳定、专家是否真正互补。"),
  "Bayesian network": E("风险事件先验 10%；警报在风险时触发率 80%，无风险时误报率 20%。", "P(风险|警报)=0.1×0.8/(0.1×0.8+0.9×0.2)", "警报后的风险概率约 30.8%。", "高触发率并不等于高后验，基础发生率和误报共同决定结果。"),
  "PCA / SVD": E("两个正交方向的方差分别为 4 和 1。", "第一主成分解释率=4/(4+1)", "第一主成分解释 80% 输入方差。", "80% 是对输入波动的压缩，不等于解释 80% 的未来收益。"),
  "Factor analysis": E("单因子载荷 λ=0.8，潜因子 z=1.5，特有误差 ε=0.1。", "x=0.8×1.5+0.1", "观测值 x=1.3，其中共同部分为 1.2。", "模型把大部分变化归于共同因子，但载荷与因子旋转并非唯一。"),
  "Lasso": E("标准化单变量的未惩罚系数为 0.08，软阈值 λ=0.03。", "β_lasso=sign(0.08)×max(0.08−0.03,0)", "收缩后系数为 0.05。", "若未惩罚系数绝对值小于 0.03，它会被压成零；入选仍需跨折稳定性。"),
  "Elastic Net": E("简化单变量中 z=0.08，L1=0.03，L2=0.10。", "β=(0.08−0.03)/(1+0.10)", "系数约为 0.045。", "L1 先产生稀疏，L2 再平滑收缩，适合相关变量组。"),
  "ARD / Sparse Bayes": E("某权重的后验精度 α=100。", "先验/后验方差尺度 α⁻¹=1/100", "该权重方差尺度为 0.01，被强烈收缩。", "较大的 α 表示数据缺少支持；不能只按后验均值是否为零判断相关性。"),
  "KNN": E("当前样本三个最近邻未来收益为 2%、−1%、3%，使用等权回归。", "ŷ=(2−1+3)%/3", "KNN 预测约 1.33%。", "结果完全依赖邻居与距离；换一种缩放可能得到不同邻居。"),
  "Kernel ridge": E("简化情形 K=I，λ=0.25，标签 y=(1,−1)。", "α=(I+0.25I)⁻¹y=0.8y", "核权重 α=(0.8,−0.8)。", "正则把核系数收缩 20%；一般情形还要由完整核矩阵传播相似性。"),
  "SVM / SVR": E("正样本的间隔 y·wᵀx=0.6。", "hinge loss=max(0,1−0.6)", "间隔损失 = 0.4。", "样本已被正确分类，但仍位于单位间隔内，因此仍产生惩罚。"),
  "Gaussian process": E("某点后验预测均值为 1.2%，标准差为 0.8%。", "约 95% 区间≈1.2%±1.96×0.8%", "区间约为 [−0.37%, 2.77%]。", "均值为正但区间跨零，说明当前数据并不足以支持强行动。"),
  "KDE / local regression": E("一维样本为 −1 和 1，在 x=0 处使用标准高斯核、h=1。", "两项核值均约 0.242；p̂(0)=(0.242+0.242)/2", "密度估计 p̂(0)≈0.242。", "带宽减小会在两端形成更尖峰，增大会把分布抹平。"),
  "CART / tree ensemble": E("候选切分后的左右叶损失为 0.02 和 0.03，切分前损失为 0.12。", "切分后总损失=0.02+0.03", "损失降至 0.05，改善 0.07。", "是否保留切分还要考虑叶样本数、复杂度惩罚和折外改善。"),
  "Random forest": E("四棵回归树预测为 1%、2%、−0.5%、1.5%。", "ŷ=(1+2−0.5+1.5)%/4", "森林预测 = 1%。", "平均降低单树方差；若四棵树高度相关，分散收益会很有限。"),
  "Gradient boosting": E("当前集成预测 0.20，新树拟合修正量 0.40，学习率 η=0.10。", "F_new=0.20+0.10×0.40", "新预测 = 0.24。", "小学习率只迈一步，需要更多轮；轮数必须在训练内部早停。"),
  "Voting / stacking": E("两个折外模型预测 2% 和 1%，组合权重为 0.6/0.4。", "ŷ=0.6×2%+0.4×1%", "组合预测 = 1.6%。", "权重必须用折外预测学习；训练内预测会让二层模型偏爱过拟合模型。"),
  "Neural network": E("单个神经元输入 x=(2,−1)，权重 w=(0.5,0.2)，偏置 −0.1，激活 ReLU。", "z=0.5×2+0.2×(−1)−0.1=0.7；h=max(0,0.7)", "隐藏表示 h=0.7。", "深层网络重复这种变换；参数规模增加后必须用强基准和严格正则。"),
  "Markov chain": E("当前处于平静状态，转移到平静/压力状态概率为 85%/15%。", "P(z_t=压力|z_{t−1}=平静)=0.15", "下一期压力概率 = 15%。", "这是条件切换概率，不考虑更久历史；若持续期明显非几何分布需扩展模型。"),
  "Hidden Markov model": E("预测先验为平静 70%、压力 30%；当前观测似然分别为 0.2/0.6。", "压力后验=0.3×0.6/(0.7×0.2+0.3×0.6)", "过滤后的压力概率 = 56.25%。", "新观测把压力概率从 30% 更新到 56.25%；实时只能用过滤结果。"),
  "Kalman filter": E("先验状态 100，先验方差 P=4；新观测 102，观测噪声 R=5。", "K=4/(4+5)=0.444；x̂=100+0.444×(102−100)", "更新状态约 100.89。", "观测被部分吸收；R 越大，滤波器越不相信新观测。"),
  "EKF / UKF": E("状态转移 f(x)=x²，当前状态均值 x=0.9。", "EKF 预测均值近似 f(0.9)=0.81，Jacobian f′(0.9)=1.8 用于传播方差", "预测中心为 0.81，局部放大系数为 1.8。", "强弯曲区域中局部线性化可能偏差明显，UKF 会用 sigma points 传播。"),
  "Particle filter": E("三个粒子状态为 0、1、3，归一化权重为 0.1、0.2、0.7。", "后验均值=0×0.1+1×0.2+3×0.7", "状态后验均值 = 2.3。", "70% 权重集中在第三粒子；还应计算 ESS 判断是否需要重采样。"),
  "Importance sampling": E("从提议分布得到 f 值 10、2、1，对应归一化权重 0.1、0.4、0.5。", "E≈0.1×10+0.4×2+0.5×1", "加权期望 = 2.3。", "第一个样本虽极端但权重较小；若单个权重接近 1，估计会非常不稳定。"),
  "Gibbs sampling": E("当前其他变量给定时，某二元状态完整条件概率 P(x_j=1|x_−j)=0.65。", "抽取 u~Uniform(0,1)；若 u=0.42<0.65，则令 x_j=1", "本轮该变量更新为 1。", "一次更新不是后验结论；需要许多轮、多链和有效样本量诊断。"),
  "Metropolis-Hastings": E("对称提议下，当前点未归一化密度为 1.0，候选点为 0.7。", "α=min(1,0.7/1.0)", "接受概率 = 70%。", "即使候选密度更低仍可能接受，从而避免链只停在局部高点。"),
  "Hamiltonian Monte Carlo": E("数值积分前后 Hamiltonian 分别为 2.40 和 2.45。", "α=min(1,exp[−(2.45−2.40)])", "接受概率约 95.1%。", "小能量误差带来高接受率；若频繁 divergence，不能靠接受率掩盖几何问题。"),
  "Variational inference": E("候选近似 q1、q2 相对真实后验的 KL 分别为 0.12 和 0.05。", "q*=arg min{0.12,0.05}", "选择 q2。", "较小 KL 只说明在所选近似族内更好；近似族本身可能漏掉尾部或多峰。"),
  "路径 Monte Carlo": E("五条路径最大回撤为 8%、12%、19%、25%、31%。", "95% 分位数用相邻次序统计量插值，约位于 25% 与 31% 之间", "P95 最大回撤约 29.8%。", "仓位应能承受接近 30% 的悲观路径，而不是只看中位数 19%。"),
  "Spectral / hierarchical clustering": E("相似图的排序特征值为 0、0.05、0.12、0.90、1.10。", "前三个较小特征值后出现明显 eigengap：0.90−0.12=0.78", "图结构提示约 3 个连通群组。", "eigengap 只是候选簇数证据，仍要跨窗口和外部组合指标验证。"),
  "Graphical lasso": E("两个变量的经验关联项为 0.15，L1 阈值 λ=0.20。", "简化软阈值：sign(0.15)×max(0.15−0.20,0)", "该条件依赖边被收缩为 0。", "真实优化作用于整个正定精度矩阵；零边仍只代表模型假设下的条件独立。"),
  "Topic / latent variable models": E("一篇文档 10 个词中，6 个当前归于 DeFi 主题；对称 Dirichlet 伪计数为 1、1。", "主题比例均值=(6+1)/(10+2)", "DeFi 主题占比约 58.3%。", "这是软主题暴露；主题名称来自人工解释，不能由未来收益反向命名。"),
  "Causal DAG learning": E("原始相关回归给出 X 对 Y 影响 0.08；控制共同原因 Z 后系数为 0.03。", "调整估计比较 E[Y|X,Z]，而不是直接比较 E[Y|X]", "在所假设 DAG 下，候选直接效应为 0.03。", "只有 Z 足以阻断所有后门路径时，这个调整才有因果解释。"),
  "Deep representation learning": E("编码器把 20 维订单簿特征压缩为 z=(0.4,−0.2,0.7)，预测头权重为 (0.5,0.1,0.3)。", "score=0.5×0.4+0.1×(−0.2)+0.3×0.7", "下游分数 = 0.39。", "数值可算，但 z 的经济含义未必直接可读，必须通过消融和域外测试解释。"),
  "候选登记与研究闸门": E("某候选通过数据、泄漏、样本外和成本检查，但稳定性检查失败。", "PASS = 1∧1∧1∧1∧0", "最终 PASS=0，拒绝进入下一阶段。", "闸门采用合取逻辑：漂亮平均收益不能抵消跨窗口不稳定。"),
};

export function getWorkedExample(name: string): MLWorkedExample {
  return EXAMPLES[name] ?? E("使用冻结的训练样本构造最小案例。", "按核心公式逐项代入已知量。", "得到可复核的中间结果。", "再用时间样本外数据判断结果是否能进入决策。" );
}

export const ML_WORKED_EXAMPLE_COUNT = Object.keys(EXAMPLES).length;
