# 工作区就绪记录（模板）

> 复制本文件到你的笔记或 `notes/` 目录。**每次运行验证命令后**更新本记录，留给下一位接手者。

## 基本信息

- 日期：
- 仓库路径：
- 仓库版本（`git rev-parse --short HEAD`，若可用）：

## 运行环境

| 项目 | 你的记录 |
|---|---|
| 操作系统 | |
| Python 入口 | 例如 `py`、`.venv\Scripts\python` |
| Node（若涉及前端构建） | |
| 工作目录 | 应为仓库根目录 |

## 输入与配置

- **固定样本：** `data/`、`data/dashboard/` fixture（勾选已确认存在）
- **配置文件：** `.env.example` 已读；真实密钥**未**写入仓库
- **关键变量：** `DASHBOARD_DATA_MODE`、`OPENAI_API_KEY` 当前状态

## 验证命令

| 命令 | 退出状态 | 关键输出摘要 |
|---|---|---|
| `py scripts/course.py setup` | | 首次环境初始化（若已做过可写「跳过」） |
| `py scripts/course.py verify` | | |
| `py scripts/course.py check` | | 可选；全量检查 |

## 结果分类

把观察拆成三类填写（参考第 3 讲表 3-2）：

### 成功项（事实）

### 警告项（事实 + 边界）

### 失败项（事实 + 分类）

失败分类示例：Python 入口不可用 / 依赖权限 / 数据缺失 / 测试断言失败

## 人工复核（至少一项）

- [ ] `AGENTS.md` 目录规则与仓库一致
- [ ] `.env.example` 无真实密钥
- [ ] 验证输出中「通过」与「警告」已分开记录

复核说明：

## 当前判断

### 证据（工作区证明了什么）

### 限制（工作区不能证明什么）

示例限制：不证明策略有效、不证明 LLM 有预测力、不构成投资建议

### 动作（继续 / 修复环境 / 停止并记录）
