# 研究假设卡（模板）

> 复制本文件到你的笔记或 `notes/` 目录，按字段填写。**跑命令之前**先写完「可检验版本」和「否定条件」。

## 原始想法

（用一句话写下市场直觉，允许模糊。）

示例：短期均线上穿长期均线后，价格可能继续上涨。

## 可检验版本

（把直觉改写成可以被数据反驳的问题。必须包含对象、触发、窗口、基准、指标。）

示例：在固定样本 `data/prices.csv` 中，`WEB3-DEMO/USDT` 的 3 日均线上穿 7 日均线策略，是否在收益和回撤上优于买入持有？

## 对象与样本

（对应表 2-1 的「对象」字段：资产与样本文件写在一起。）

- 对象：
- 样本文件：
- 样本说明：（固定离线 / 可复现 / 不代表真实市场）

## 触发条件

（什么事件发生后开始观察？须与可检验版本一致。）

## 观察窗口

（触发后观察多久？例如：至下一次反向信号，或至样本结束。）

## 对照基准

（和谁比？例如：买入持有。）

## 评价指标

（事先约定看哪些数。例如：策略收益（`strategy_return_pct`）、买入持有收益（`buy_hold_return_pct`）、策略最大回撤（`maximum_drawdown_pct`）、交易次数（`trade_count`）。）

## 否定条件

（分两类写清，不能为空。**不支持**：什么结果说明假设不成立；**证据不足**：什么情况下无法下结论，如交易数小于 2（`trade_count < 2`）。）

示例：**不支持** — 策略收益不优于买入持有；**证据不足** — 交易数小于 2（`trade_count < 2`）。

## 运行记录

**主假设**行的结果用于「当前判断」；**补充观察**行（若填写）不得用于推翻或支持主假设——若要正式检验另一组参数，请另开一张假设卡。

| 用途 | 参数 (short/long) | 命令 | 策略收益（`strategy_return_pct`） | 买入持有收益（`buy_hold_return_pct`） | 策略最大回撤（`maximum_drawdown_pct`） | 交易次数（`trade_count`） |
|---|---|---|---:|---:|---:|---:|
| 主假设 | 3 / 7 | `py report_cli.py --format summary` | | | | |
| 补充观察（可选） | 5 / 10 | `py report_cli.py --format summary --short 5 --long 10` | | | | |

## 当前判断

（只根据**主假设**行填写；勿用补充观察行改写本节。）

### 事实

（只写命令输出的数字和警告原文。）

### 解释

（只根据**主假设**行、对照否定条件填写；「不支持」与「证据不足」不得混用。）

### 动作

（继续、修订假设，或停止；不得偷换原始假设。若要检验 5/10，写「另开新卡」而非改结论。）
