"""Insert bridge prose between ### group headings and #### subsections."""

from __future__ import annotations

import re
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
CHAPTER_DIR = ROOT / "docs" / "v2"

H3_INTRO: dict[str, str] = {
    "从一个漂亮输出开始": (
        "先统一几个后面会反复出现的术语，再去看一段足够像专业建议的输出——"
        "正是这类结果最容易让人跳过检验、直接当作结论。"
    ),
    "建立责任边界框架": (
        "识别证据缺口之后，还要把 Codex、LLM、量化验证和人的分工写成可操作的框架。"
        "下面三步依次回答：核心词指什么、责任边界画在哪里、生成与证明为何不能混用。"
    ),
    "从分工落到仓库与验证路径": (
        "概念上的分工必须能在仓库里找到对应模块。先对照真实路径，"
        "再区分全书会出现的两类验证问题，避免把「代码在哪里」和「正在证明什么」混为一谈。"
    ),
    "读代码并运行最小路径": (
        "按「先读代码、再跑命令」的顺序，把抽象分工落到一条可执行路径上。"
        "读代码时重点不是函数多不多，而是规则基准、LLM 包装和失败回退怎样串在同一条链上。"
    ),
    "解读输出与常见误读": (
        "命令能跑出结果，不等于结果已被证明。先用表比较四类输出各自能证明什么，"
        "再单独处理最容易误读的字段。"
    ),
    "界定研究假设": (
        "下面依次说明：交易想法与假设的分界、六个必填字段，"
        "以及只写成功条件为何会让研究无法被否定。"
    ),
    "从代码到假设": (
        "按「先读路径、再跑命令、最后回到假设修订」的顺序，"
        "把上一节的字段表放进仓库的最小样本里。"
    ),
    "判断与放行": (
        "输出解读清楚之后，还要用明确的放行、退回或拒绝标准结束本轮研究，"
        "避免把「数字不好看」和「研究无效」混为一谈。"
    ),
    "复现边界": (
        "可复现工作区先把五个边界写清：目录、配置、样本、命令和失败记录。"
        "下面两项分别说明边界是什么，以及怎样从安装步骤升级到复现协议。"
    ),
    "最小实践三步": (
        "按「读代码 → 跑验证 → 解读结果」三步，检查工作区是否真的能被别人接手。"
    ),
    "五个对象与基础公式": (
        "量化语言从五个不能混用的对象开始，再对照完整系统反推基础公式。"
        "下面四段依次建立概念、说明必要性、画出地图并给出本章公式。"
    ),
    "实践与解读": (
        "概念清楚之后，用一段固定样本跑通读数路径，并练习三种不同的结果读法。"
    ),
    "认识边界": (
        "安全边界不是一句提示语，而是三层可检查的限制。"
        "下面先看容易误判的输出，再拆三层边界，最后说明为何必须落到代码里。"
    ),
    "仓库中的边界": (
        "边界不能只写在文档里。下面按产品目标、风险规则、页面语义和能力复核四处，"
        "在仓库里定位对应的实现与测试。"
    ),
    "最小实践": (
        "把一条看似像建议的输出，改写成带限制条件的研究结论。"
        "三步分别是改写输入、运行检查、解读降级后的表述。"
    ),
    "指标知识准备": (
        "在算指标之前，先建立指标家族的学习顺序，并固定本章使用的公式口径。"
    ),
    "策略规则骨架": (
        "把模糊信号写成策略，先要骨架：触发、仓位、退出与失效条件。"
        "下面两段分别给出最小骨架，以及从指标公式到规则的翻译方法。"
    ),
    "案例模板与典型策略": (
        "骨架清楚之后，用 Qbot 模板和两类典型策略说明「规则必须能逐根 K 线执行」。"
    ),
    "快速入门": "想先跑通再细读时，可以从下面的一键命令进入本章主路径。",
    "回测报告与卫生清单": (
        "回测结论要有统一的卫生清单和案例报告模板，避免只贴一张收益曲线。"
    ),
    "比较方法与报告": (
        "多策略、多窗口比较时，公平性规则与报告模板必须先于结论写清。"
    ),
    "因子挖掘定位与问题类型": (
        "因子挖掘先要问对问题：它在流程中的位置、截面与时序差异，"
        "以及可借鉴的多因子与配对结构。"
    ),
    "技术路线与仓库对照": (
        "问题类型清楚之后，对照业界路线与本仓库三层实现，避免把搜索器当成策略本身。"
    ),
    "实操走读与验收": (
        "对照清楚之后，沿沙箱模块走读、完成实操验收，并准备可委托 Codex 的检查步骤。"
    ),
    "风险因子与业界对标": (
        "收益因子之外，还要单独处理风险因子挖掘，并对标业界升级顺序。"
    ),
    "方法说明": "先区分 GP 与 ML 两类搜索方法，再进入命令与证据链。",
    "实操、证据与比较": (
        "方法说明之后，用命令跑通沙箱，沿证据链核对输出，并与多策略比较并排阅读。"
    ),
    "指标与公式速查": (
        "评价指标不是排行榜。下面先说明如何联合阅读，再给出本章公式速查。"
    ),
    "污染识别方法": "核心方法落地之前，先用一组初学者可操作的识别法对照三类污染。",
    "风控分层与公式": (
        "运行期风控按层次拆分规则，并固定仓位与止损相关的公式口径。"
    ),
    "路径边界地图": "端到端贯通前，先用边界地图标出每一段输入、输出与停止条件。",
    "专题实践三步": (
        "下面三步按「读代码 → 跑命令 → 解读结果」展开；"
        "每一步都要回到证据边界，而不是只看输出是否流畅。"
    ),
    "案例执行流程": (
        "案例按四步记录：先固定输入与预期，再沿代码路径执行，"
        "然后注入失败，最后形成可交接的判断。"
    ),
    "案例验收": (
        "流程跑通之后，用验收清单逐项确认：输入可追溯、失败可观察、结论不过度、交付可接手。"
    ),
    "组件定位与实验设计": (
        "先把 LLM/数据/自动化在研究链中的位置写清，"
        "再固定可复现实验协议与真实输出样本。"
    ),
    "复核与发布决定": (
        "实验输出必须经过关键计算与人工复核，再由人决定是否继续、修改或停止。"
    ),
    "复盘与配套资产": (
        "最后把容易自欺之处写成研究者笔记，并保留配套图解与代码供下一位研究者复查。"
    ),
    "口径定义与复算公式": (
        "严谨性检查从两类硬口径开始：研究合同（证明什么）和最小复算公式（怎样核对程序输出）。"
        "先固定变量与公式，再说明样本与成本假设为何不能省略。"
    ),
    "偏差控制与人工复核": (
        "口径写清之后，主动排查偏差来源，并至少做一次人工复核——"
        "确认机器检查的是正确语义，而不是只跑通成功路径。"
    ),
    "失败注入与综合示例": (
        "最后一组动作是主动制造失败、观察系统是否诚实，"
        "再用总图或示例代码把全讲路径收成可复查的交付物。"
    ),
    "补充示例与记录": (
        "下面用示例和记录把本章检查动作收成可复查、可交接的交付物。"
    ),
}

H4_PAIR: dict[tuple[str, str], str] = {
    ("前置术语简释", "漂亮输出为什么危险"): (
        "术语统一之后，用一段结构化输出作为本章的「压力测试对象」："
        "它看起来像结论，但每一项字段都还需要追问来源与证明边界。"
    ),
    ("本书里的几个核心词指什么", "四类角色的责任边界"): (
        "概念定义清楚之后，用一张表把四类角色各自的证据边界固定下来。"
    ),
    ("从抽象分工回到文件", "两条量化验证路径"): (
        "路径对照清楚之后，区分「规则已知」与「规则未知」两条验证路径——"
        "搜索出来的因子与回测跑出来的收益，证明对象并不相同。"
    ),
    ("源码核心逻辑走读", "本地运行与标准输出示例"): (
        "逻辑看清楚之后，在项目根目录运行命令，观察实际输出与回退行为。"
    ),
    ("四类输出的证明边界推演", "`confidence` 字段的典型误读陷阱"): (
        "四类边界分清之后，单独处理同名不同义的 `confidence` 字段——"
        "这是最容易把表达强度误读成盈利概率的地方。"
    ),
    ("为什么交易想法不等于研究假设", "六个必填字段"): (
        "分界清楚之后，把想法压缩成六个必填字段；"
        "每补一个字段，都是在减少一次「让模型猜」的机会。"
    ),
    ("六个必填字段", "只写成功条件的危险"): (
        "六个字段里，最容易被省略的是否定条件。"
        "很多看似严谨的计划，问题正出在只规定怎样算成功、却不规定怎样承认失败。"
    ),
    ("代码走读", "实战推演"): (
        "看清最小证据路径之后，再运行命令，让假设接受第一次数据反驳。"
    ),
    ("实战推演", "从输出到假设修订"): (
        "命令输出不是终点。下面说明如何把一组数字转写为假设修订，而不是事后挑指标。"
    ),
    ("从输出到假设修订", "结果解读"): (
        "假设修订写清之后，用图把放行、退回与拒绝三种决定固定成可执行标准。"
    ),
    ("结果解读", "放行、退回与拒绝"): (
        "解读完毕，最后对照放行、退回与拒绝的判定条件，结束本轮最小实践。"
    ),
    ("变量与公式", "样本口径与成本假设"): (
        "公式可以复算，还依赖样本窗口、频率和成本假设是否事先写清。"
        "下面说明这些口径，以及为什么不能把「成本未建模」写成「成本为零」。"
    ),
    ("偏差来源与反例", "最小人工复核"): (
        "偏差检查不能替代人工复核。下面用表列出至少应核对的一项数值或字段。"
    ),
    ("最小人工复核", "常见失败写法"): (
        "复核通过之后，还要对照常见失败写法，避免把弱证据写成强结论。"
    ),
    ("五个边界", "从安装步骤到复现协议"): (
        "五个边界明确之后，下面说明怎样从「我这里能运行」升级到「他人可复现」。"
    ),
    ("代码走读", "运行验证"): (
        "看清目录与命令入口之后，运行验证命令，检查工作区是否满足复现合同。"
    ),
    ("运行验证", "结果解读"): (
        "验证命令跑通或失败后，都要解读：通过证明什么、失败指向哪一段证据链断裂。"
    ),
    ("五个对象不能混用", "为什么这不是“基础课废话”"): (
        "五个对象分开之后，还要说明为什么混用它们会直接破坏后面的回测与风控解释。"
    ),
    ("为什么这不是“基础课废话”", "从完整量化系统反推基础地图"): (
        "概念必要性的后面，用一张地图把价格、收益、信号和仓位放回完整系统里对照。"
    ),
    ("从完整量化系统反推基础地图", "学习资料中的基础公式放在本章"): (
        "地图清楚之后，固定本章要用的基础公式口径，供后续手工复算。"
    ),
    ("容易误判的输出", "三层边界"): (
        "先看什么样的输出最容易越界，再把允许、审批与禁止三层边界拆开说明。"
    ),
    ("三层边界", "为什么边界要落到代码里"): (
        "三层边界不能只写在文档里；下面说明为何必须落到代码、测试与页面语义里。"
    ),
    ("产品目标边界", "风险规则边界"): (
        "产品目标划清之后，继续定位运行时风险规则怎样拦截越界动作。"
    ),
    ("风险规则边界", "页面语义边界"): (
        "规则之外，页面文案也必须把「研究结论」和「实盘执行」区分开。"
    ),
    ("页面语义边界", "能力边界复核"): (
        "页面语义检查之后，最后复核系统能力清单，确认没有隐藏的真实交易入口。"
    ),
    ("输入改写", "运行检查"): (
        "输入降级为研究表述之后，运行检查命令，确认边界是否被测试覆盖。"
    ),
    ("运行检查", "结果解读"): (
        "检查完成之后，解读输出是否仍含越界暗示，以及应如何改写结论表述。"
    ),
    ("常见指标家族与学习顺序", "常用技术指标公式速查"): (
        "知道指标家族与学习顺序之后，固定本章使用的公式速查，便于后续复算。"
    ),
    ("策略规则的最小骨架", "从指标公式到策略规则"): (
        "骨架列出之后，下面说明怎样把指标公式翻译成可逐 bar 执行的规则。"
    ),
    ("Qbot 案例拆解模板", "拐点交易：把策略写成状态机"): (
        "模板清楚之后，用拐点交易示例说明怎样把策略写成可检验的状态机。"
    ),
    ("拐点交易：把策略写成状态机", "网格交易：把仓位写成价格函数"): (
        "状态机示例之后，再用网格交易说明仓位怎样写成显式的价格函数。"
    ),
    ("案例步骤一：保存输入与预期", "案例步骤二：沿代码路径执行"): (
        "输入与预期写进记录之后，沿公开入口进入实现，而不是直接改最终输出。"
    ),
    ("案例步骤二：沿代码路径执行", "案例步骤三：注入失败"): (
        "正常路径核对完毕，再主动注入本章设计的失败，观察系统是否停下并说明原因。"
    ),
    ("案例步骤三：注入失败", "案例步骤四：形成判断"): (
        "失败可观察之后，整理观察、解释与决定，形成下一位研究者能接手的判断。"
    ),
    ("失败注入与恢复", "一个最小示例"): (
        "失败注入检验系统能否停下；下面用总图和示例代码把全讲路径收成可携带的检查卡片。"
    ),
    ("把 LLM 当作受测组件，而不是答案来源", "使用真实输出建立判断"): (
        "组件定位清楚之后，用仓库里的真实输出建立判断，而不是凭单次演示下结论。"
    ),
    ("LLM 信号必须经过策略化和历史检验", "使用真实输出建立判断"): (
        "策略化与历史检验的边界清楚之后，用真实输出建立判断，而不是凭单次演示下结论。"
    ),
    ("数据生成过程决定结论上限", "使用真实输出建立判断"): (
        "数据生成过程决定结论上限；下面用真实输出建立判断，而不是凭单次演示下结论。"
    ),
    ("页面必须展示证据链，而不是制造交易冲动", "使用真实输出建立判断"): (
        "页面语义固定之后，用真实输出建立判断，而不是凭界面流畅度下结论。"
    ),
    ("自动化的评价对象是流程，而不是单次回答", "使用真实输出建立判断"): (
        "自动化评价流程而非单次回答；下面用真实输出建立判断，而不是凭一次成功下结论。"
    ),
    ("综合系统中的 LLM 与量化闭环", "使用真实输出建立判断"): (
        "闭环边界清楚之后，用真实输出建立判断，而不是凭端到端演示下结论。"
    ),
    ("使用真实输出建立判断", "本章可复现实验协议"): (
        "真实样本之后，把实验步骤写成他人可重复的协议：输入、命令、指标与停止线。"
    ),
    ("关键计算与人工复核", "从实验结果到发布决定"): (
        "关键量复算无误之后，再由人根据证据厚度决定继续、修改还是停止。"
    ),
    ("从实验结果到发布决定", "研究者笔记：如何避免在本章自欺"): (
        "发布决定不能代替复盘。下面记录本章最容易自欺的地方，供下一轮对照。"
    ),
    ("研究者笔记：如何避免在本章自欺", "进一步推导：从 LLM 指标到策略价值"): (
        "复盘笔记之后，单独说明指标改善与策略价值之间仍需要哪几道额外检验。"
    ),
    ("进一步推导：从 LLM 指标到策略价值", "给学员的复盘要求"): (
        "推导边界清楚之后，把本章要求收束为学员交付时必须满足的复盘项。"
    ),
    ("给学员的复盘要求", "配套图解与代码示例"): (
        "复盘要求固定之后，用配套图解与代码示例把本章路径留在可复查的资产里。"
    ),
    ("研究假设卡示例", "它怎样连接后续章节"): (
        "示例卡片之后，说明本章假设怎样接入工作区与后续回测章节。"
    ),
    ("两类方法", "一键运行"): (
        "方法区分清楚之后，用下面命令在固定教学样本上跑通沙箱。"
    ),
    ("多策略比较的公平性规则", "多窗口回测案例报告模板"): (
        "公平性规则写清之后，用报告模板固定多窗口比较的交付格式。"
    ),
    ("21.8.1 因子挖掘在研究流程中的位置", "21.8.2 截面 vs 时序：先选对问题"): (
        "定位清楚之后，先区分截面与时序两类问题，避免用错评价指标。"
    ),
    ("21.8.2b 配对交易资料的可借鉴图形", "21.8.3 业界四条技术路线"): (
        "问题类型与借鉴结构清楚之后，对照业界四条技术路线。"
    ),
    ("21.8.4 本仓库三层对照", "21.8.5 沙箱模块走读"): (
        "路线与仓库对照清楚之后，沿沙箱模块走读实现细节。"
    ),
    ("21.8.8 常见翻车", "21.8.10 风险因子挖掘（RIC → 仓位缩放）"): (
        "常见翻车记录清楚之后，单独处理风险因子挖掘与仓位缩放预览。"
    ),
    ("21.8.10 风险因子挖掘（RIC → 仓位缩放）", "21.8.9 若要对标业界的升级顺序"): (
        "风险因子路径之后，说明若要对标业界时的升级顺序与边界。"
    ),
    ("回测基础卫生清单", "单次回测案例报告模板"): (
        "卫生清单固定之后，用报告模板约束单次回测的交付格式。"
    ),
    ("指标不是排行榜", "回测评价公式速查"): (
        "联合阅读原则清楚之后，固定本章使用的评价公式速查。"
    ),
    ("风控规则的基础分层", "仓位与风控公式速查"): (
        "分层清楚之后，固定仓位与风控相关的公式速查，便于复算与对照。"
    ),
    ("模拟交易路径的边界地图", "在配套仓库中找到真实实现"): None,
}


def chapter_number(path: Path) -> int | None:
    match = re.match(r"^(\d+)-", path.name)
    return int(match.group(1)) if match else None


def heading_title(line: str) -> tuple[int, str] | None:
    match = re.match(r"^(#{2,4}) (.+?)\s*$", line)
    if not match:
        return None
    return len(match.group(1)), match.group(2)


def next_nonempty_index(lines: list[str], start: int) -> int | None:
    for i in range(start, len(lines)):
        if lines[i].strip():
            return i
    return None


def next_heading_index(lines: list[str], start: int) -> int | None:
    for i in range(start, len(lines)):
        if lines[i].lstrip().startswith("#"):
            return i
    return None


def paragraph_before(lines: list[str], index: int) -> str:
    pos = index - 1
    while pos >= 0 and not lines[pos].strip():
        pos -= 1
    parts: list[str] = []
    while pos >= 0 and lines[pos].strip() and not lines[pos].lstrip().startswith("#"):
        parts.insert(0, lines[pos].strip())
        pos -= 1
    return " ".join(parts)


def has_bridge(text: str) -> bool:
    if not text:
        return False
    tail = text[-120:]
    if re.search(r"(?:下面|接下来|下一步|依次|先.{0,12}再|最后一组)", tail):
        return True
    return any(
        phrase in tail
        for phrase in (
            "下面依次",
            "下面三步",
            "下面四",
            "下面用",
            "下面说明",
            "下面固定",
            "下面区分",
            "下面按",
            "案例按四步",
            "流程跑通之后",
            "术语统一之后",
            "路径对照清楚之后",
            "公式可以复算",
            "偏差检查不能替代",
        )
    )


def insert_bridge(lines: list[str], before_index: int, paragraph: str) -> None:
    lines.insert(before_index, "")
    lines.insert(before_index + 1, paragraph)
    lines.insert(before_index + 2, "")


def process_lines(lines: list[str]) -> bool:
    changed = False
    i = 0
    while i < len(lines):
        parsed = heading_title(lines[i])
        if not parsed:
            i += 1
            continue
        level, title = parsed
        nxt = next_heading_index(lines, i + 1)
        if nxt is None:
            i += 1
            continue

        nxt_parsed = heading_title(lines[nxt])
        if not nxt_parsed:
            i += 1
            continue

        if level == 3 and nxt_parsed[0] == 4:
            between_text = " ".join(
                line.strip() for line in lines[i + 1 : nxt] if line.strip()
            )
            intro = H3_INTRO.get(title)
            if intro and not between_text:
                insert_bridge(lines, nxt, intro)
                changed = True
                i = nxt + 1
                continue

        if level == 4 and nxt_parsed[0] == 4:
            bridge = H4_PAIR.get((title, nxt_parsed[1]))
            prev = paragraph_before(lines, nxt)
            if bridge and not has_bridge(prev) and bridge[:24] not in prev:
                insert_bridge(lines, nxt, bridge)
                changed = True
                i = nxt + 1
                continue

        i += 1
    return changed


def process_file(path: Path) -> bool:
    num = chapter_number(path)
    if num is None or num in {0, 1}:
        return False
    text = path.read_text(encoding="utf-8")
    lines = text.splitlines()
    changed = process_lines(lines)
    if changed:
        path.write_text("\n".join(lines) + ("\n" if text.endswith("\n") else ""), encoding="utf-8")
    return changed


def main() -> int:
    updated: list[str] = []
    for path in sorted(CHAPTER_DIR.glob("[0-9][0-9]-*.md")):
        if "修订对照" in path.name:
            continue
        if process_file(path):
            updated.append(path.name)
    if updated:
        print("Added heading transitions in:")
        for name in updated:
            print(f"  - {name}")
    else:
        print("No files needed transition updates.")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
